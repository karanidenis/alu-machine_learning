-- create first table in db_0, 
-- first_table:
-- id: int
-- name: varchar(256)

-- db name will be passed as an argument of mysql command

CREATE TABLE IF NOT EXISTS first_table (
  id INT,
  name VARCHAR(256)
);

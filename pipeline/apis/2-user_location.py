#!/usr/bin/env python3

"""
This module contains a function that
uses Github API to print location of
specific users"""

import requests

# url should come after filename when running the file
# username should be part of the url
# response = requests.get(url)
# data = response.json()
# print(data['location'])
